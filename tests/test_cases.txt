Language Features:
    Arithmetic operators
    String Operations
    Logical Operators
    if & If else
    While loops, with break and continue
    Block Scoping
    Functions
    Recursion
    Functions as Objects

Type Checking:
    Assigning different types to variable with any type
    Do the same with an annotated variable

    Binary operations on 2 variables with 'any' types vs with annotated types

    passing differently annotated variables into a function with and another without arg annotations

    function that returns "any" assigned to a variable that contains "num".. later used as a bool

    lambda signature doesnt matter.. if variable has no signature. 
    
     


    